import React from 'react';
import ReactDOM from 'react-dom';

import WStoreApp from './WStoreApp';

ReactDOM.render(
    <React.StrictMode>
        <WStoreApp/>
    </React.StrictMode>,
    document.getElementById('root')
);